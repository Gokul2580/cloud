 Update system

sudo apt update

2️⃣ Install compiler

sudo apt install build-essential

3️⃣ Verify

gcc --version


---

🐳 Install Docker on Ubuntu

1️⃣ Update packages

sudo apt update && sudo apt upgrade -y

2️⃣ Install deps

sudo apt install ca-certificates curl gnupg

3️⃣ Add Docker key

sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg

4️⃣ Add repo

echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

5️⃣ Update

sudo apt update

6️⃣ Install Docker

sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

7️⃣ Verify

docker --version

8️⃣ Allow running without sudo

sudo usermod -aG docker $USER

(Logout + login once)

9️⃣ Test container

docker run hello-world


---

🧪 Sample C Program

Create file: hello.c

#include <stdio.h>

int main() {
    printf("Hello, World 👋\n");
    return 0;
}

Compile

gcc hello.c -o hello

Run

./hello

Output:

Hello, World 👋

Cool bro, agora full walkthrough pannuren — install → test → run real container. Follow this step-by-step, tension illa 😎👇


---

🐳 Step 1 — Update System

sudo apt update && sudo apt upgrade -y


---

🔧 Step 2 — Install Required Packages

sudo apt install ca-certificates curl gnupg


---

🔑 Step 3 — Add Docker GPG Key

sudo install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
sudo chmod a+r /etc/apt/keyrings/docker.gpg


---

📦 Step 4 — Add Docker Repo

echo \
"deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
$(. /etc/os-release && echo "$VERSION_CODENAME") stable" | \
sudo tee /etc/apt/sources.list.d/docker.list > /dev/null


---

📥 Step 5 — Install Docker Engine

sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin -y


---

🔍 Step 6 — Verify Installation

docker --version

Expected something like:

Docker version 27.xx.x


---

👤 Step 7 — Allow Running Docker Without sudo

sudo usermod -aG docker $USER

➡️ IMPORTANT: Logout → Login once
(illena permission work aagathu)


---

🧪 Step 8 — Test Docker

docker run hello-world

If everything is correct, output like this varum:

Hello from Docker!

🔥 That means Docker working perfectly.


---

🚀 Step 9 — Run a Real Container (Ubuntu inside Ubuntu)

docker run -it ubuntu bash

Now u inside container ↓
(ls, pwd, apt install pannalam)

Exit panna:

exit


---

🎯 Step 10 — Run a Web Server Example

docker run -d -p 8080:80 nginx

Then browser open → http://localhost:8080
→ NGINX welcome page varum 🥵🔥


---

☑️ Checklist

Task	Status

Installed Docker	✅
Run hello-world	✅
Run Ubuntu Shell container	✅
Run NGINX webserver	✅



---

If u want next stage:

Dockerfile create

Images build

Docker Compose setup

Host website inside Docker


Just tell:

👉 "Next level solderu da"

Ready? 😈🔥

Ok cool, this one proper workflow, so step-by-step clean ah kudukuren da.
Target: Install Google App Engine (GAE) SDK → create sample app → run locally in Ubuntu VM.


---

🧩 Step 1 — Update Ubuntu

sudo apt update && sudo apt upgrade -y


---

🧲 Step 2 — Install Python + Pip (GAE uses it)

sudo apt install python3 python3-pip -y

Check:

python3 --version
pip3 --version


---

☁️ Step 3 — Install Google Cloud CLI (GCLOUD)

This one required because App Engine deploy/run depends on it.

Download key:

curl -O https://packages.cloud.google.com/apt/doc/apt-key.gpg

Add repo:

sudo apt-get install apt-transport-https ca-certificates gnupg -y
echo "deb [signed-by=/usr/share/keyrings/cloud.google.gpg] http://packages.cloud.google.com/apt cloud-sdk main" | \
sudo tee /etc/apt/sources.list.d/google-cloud-sdk.list > /dev/null

Add key:

sudo install -m 644 apt-key.gpg /usr/share/keyrings/cloud.google.gpg

Update + install:

sudo apt update && sudo apt install google-cloud-sdk -y

Verify:

gcloud --version


---

🔐 Step 4 — Login to Google Account

gcloud auth login

Browser open aagum → login → approve.


---

🏗️ Step 5 — Enable App Engine

Choose region (ex: asia-south1 for India support):

gcloud app create --region=asia-south1


---

🚀 Step 6 — Install App Engine Python Components

gcloud components install app-engine-python


---

🧪 Step 7 — Create Sample App

Create folder:

mkdir myapp && cd myapp

Create file:

app.yaml

runtime: python310
entrypoint: gunicorn -b :$PORT main:app

Create:

requirements.txt

Flask==3.0.0
gunicorn==21.2.0

Then mainfile:

main.py

from flask import Flask
app = Flask(__name__)

@app.route("/")
def home():
    return "Hello from Google App Engine 😎🔥"

if __name__ == "__main__":
    app.run(host="0.0.0.0", port=8080)


---

⏳ Step 8 — Install dependencies

pip3 install -r requirements.txt


---

🧪 Step 9 — Run App Locally

python3 main.py

Browser open:

👉 http://localhost:8080

You should see:

Hello from Google App Engine 😎🔥


---

🌍 Step 10 — Deploy to Google Cloud

gcloud app deploy

Then access live:

gcloud app browse


---

📌 Summary Flow

Step	Status

Install Python	✔️
Install gcloud	✔️
Init App Engine	✔️
Create Flask app	✔️
Run local	✔️
Deploy cloud	✔️



---

If you want, next I can teach:

Custom domain setup

Firestore + App Engine integration

CI/CD auto deploy

Dockerizing the project


Just say:

👉 “Level 2 podu da.” 😈🔥
